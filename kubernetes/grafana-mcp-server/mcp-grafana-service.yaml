apiVersion: v1
kind: Service
metadata:
  name: mcp-grafana
  namespace: observability
spec:
  type: NodePort
  selector:
    app: mcp-grafana
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30090
      protocol: TCP

